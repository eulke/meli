import React from 'react';
import { Input } from './Input';

export default {
  component: Input,
  title: 'Input',
};

export const input = () => {
  return <Input placeholder="Nunca dejes de buscar" />;
};
